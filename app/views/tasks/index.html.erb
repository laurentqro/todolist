<h1>My to-do list</h1>

<p><%= link_to "New task", new_task_url, class: "btn btn-primary" %></p>

<hr>

<% @tasks.each do |task| %>
  <div class="well well-sm">
  <%= task.description %> <%= link_to "Delete", task_url(task), method: "delete", class: "pull-right" %>
  </div>
<% end %>

